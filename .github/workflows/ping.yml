name: Keep Streamlit App Awake

on:
  schedule:
    - cron: '*/60 * * * *'  # every 60 mins
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Curl Streamlit App
        run: curl -s -o /dev/null -w "%{http_code}" https://aymang.streamlit.app/
